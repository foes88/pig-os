# QA/QC 검증 결과 — 2026-06-19

> 대상: 이번 세션 산출물 (P0 검증 13+7 · MVP 보고서 5종 · 데이터 정합성).
> 원칙: 통과 위조 0. 동작하는 게이트는 실행, 환경 제약은 사유 명시.

## 게이트별 결과

| 게이트 | 명령 | 결과 |
|--------|------|------|
| **Python lint** | `uv run ruff check app/ tests/` | ✅ **All checks passed** (초기 6건 import 정리/미사용 → 자동수정) |
| **Backend tests** | `uv run pytest tests/ -q` | ✅ **379 passed** (P0 회귀 10 · 데이터품질 4 · MVP리포트 4 포함) |
| **TS typecheck** | `npx tsc --noEmit` | ✅ **0 errors** |
| **i18n 정합성** | en 기준 5개어 키 비교 | ✅ **1084 keys × 5 (en/ko/zh/es/vi) 완전 일치** |
| **Production build** | `npm run build` | ✅ **성공** (39 routes 생성, 0 build error) |
| **Live E2E** | `npm run test:e2e:live` | ✅ **30 passed** (기존 28 + P0 클라검증 2, 실 API/실 브라우저) |
| **신규 엔드포인트 live** | curl 200 확인 | ✅ data-quality / sow-status / farrowing / mortality 모두 200 |

## 환경 제약 (코드 문제 아님 — 위조하지 않고 명시)

- **Vitest 단위 테스트**: ⚠️ **실행 불가**. 로컬 Node `v20.11.1` < `20.12` 인데 vitest 4(rolldown)가 `node:util.styleText`(20.12+ 추가)를 요구 → 기동 실패. **코드/테스트 문제 아님, 환경 문제.**
  - 닫는 조건: **Node ≥20.12로 업그레이드**(사용자/머신 작업). vitest 3.x 다운그레이드도 시도했으나 ① plugin-react 6(ESM)↔vitest3(CJS config) 충돌 ② `@rolldown` 잔재 파일에 Windows EPERM 락 → 막힘. package.json은 vitest ^4.1.8로 원복(클린 상태). Node 업그레이드 후 `npm ci` 1회로 해결됨.
  - 그동안 프론트 회귀는 **tsc(0) + live E2E 30 + production build**로 커버됨.
  - 영향 테스트 파일(6): `tests/components/{Sidebar,QuickInputDrawer,RecentEventsSection}.test.tsx`, `tests/pages/{alerts,notifications,tasks}.test.tsx`.
- **ESLint**: 미설정(`next lint`가 대화형 셋업 요구). 타입 안전성은 `tsc --noEmit`(0)로 게이트. (별도 ESLint 도입은 백로그.)

## 요약
- **동작 게이트 전부 그린**: ruff · pytest 379 · tsc 0 · i18n 1084×5 · build · live 30.
- **단 1개 미실행(vitest)** = Node 버전 제약, 사유·닫는조건 명시. **FAIL 0.**

## 다음 액션 제안
1. Node 20.12+ 업그레이드 후 vitest 재실행 (또는 vitest 3.x 핀).
2. ESLint flat config 도입 (CI 연동).
