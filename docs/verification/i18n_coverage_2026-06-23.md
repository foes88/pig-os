# i18n 커버리지 감사 — 2026-06-23 (7개 언어)

> 7개 로케일 = 공개 6개(en·zh·es·vi·**th**·**pt**) + 한국어(ko, 플랫폼 관리자 전용).
> 신규 추가: 태국어(th)·브라질 포르투갈어(pt). 모든 화면 **크래시 0·원시 i18n 키 0**(미번역은 영어 폴백).

## 인프라 (완료)
- locales 7개: `i18n/config.ts`, `middleware.ts`(detectLocale), `request.ts`(자동)
- 스위처: Topbar(LANG_LABELS th=ไทย/pt=PT) · 로그인 페이지(LANGS+T 객체 th/pt 실번역)
- chrome 컴포넌트 타입 `Partial<Record<Locale>> & {en}` + `?? .en` 폴백 → 신규 로케일 안전
- 챗(AskAiDrawer): th/pt는 렌더러 en으로 매핑(Addon, 추후 확장)

## th/pt 실번역 완료 (메시지 7 네임스페이스 — 전 화면 공통/고가시)
`nav · auth · errors · common · sowStatus · validation · notifications`
→ 메뉴·로그인·상태배지·버튼·오류·검증 메시지·알림은 태국어/포르투갈어로 표시됨.
+ 로그인 페이지 인라인 문구(제목/이메일/비번/CTA 등) th/pt 실번역.

## 영어 폴백 (전문/추가 번역 대기 — 42 네임스페이스)
화면 표시는 정상(영어), 크래시 없음. 우선순위순 번역 권장:
- **1순위(고사용 화면)**: dashboard, sows, record, kpi, alerts, reports, events, settings, finishers, boars, piglets
- **2순위**: farrowing, sowDetail, eventHistory, reproReport, growFinish, dailyReport, comprehensiveDaily, dataQuality, mortalityReport, farrowingReport, sowStatusReport, ledger, tasks, profile, settingsFarm, benchmarks, thresholds, insights, chat, announcements, support, users
- **운영자 전용(후순위)**: admin, alertsMeta, addons, billing, deleteAccount, legal, prrsReport, upgrade, util

## 인라인 chrome 라벨 (현재 en 폴백 — 번역 권장)
- `Sidebar.tsx` 메뉴 라벨(~22개): th/pt 미설정 → en 표시(폴백 안전). 고가시라 우선 번역 권장.
- `QuickInputDrawer.tsx` 이벤트 라벨, `AlertsTabs`/`ReportsTabs` 탭 라벨: en 폴백.

## 검증
- tsc 0 · build 50 pages · live E2E 34 passed(7개 로케일 raw키 0 + 스위처 반영) · pytest 417.

## 다음
- 1순위 네임스페이스 + Sidebar 라벨 th/pt 번역(네임스페이스별 진행 가능).
- 법무/약관(legal)·보도자료는 전문 번역 권장.
